This file is generated by `ticket-to-task --agent cursor`.

Run:

```bash
npx tsx src/cli/cli.ts ticket-to-task <issueNumber> --agent cursor
```

The generated version includes the GitHub issue URL, relevant files, roadmap context, acceptance criteria, and a Cursor-oriented implementation prompt.
